# defines a function that, given the valiu of
# the three sides of a triangle, retunrs the
# lenght of its perimeter

def perimeter_triangle(s1, s2, s3):
    return s1 + s2 + s3


print(perimeter_triangle(12, 34, 12))
